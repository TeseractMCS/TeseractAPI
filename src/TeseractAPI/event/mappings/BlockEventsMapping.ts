import PlayerBreakBlockEvent from "../block/PlayerBreakBlockEvent";
import PlayerPlaceBlockEvent from "../block/PlayerPlaceBlockEvent";

const BlockEventsMapping = {
    PlayerBreakBlockEvent: PlayerBreakBlockEvent,
    PlayerPlaceBlockEvent: PlayerPlaceBlockEvent,
};

export default BlockEventsMapping;
